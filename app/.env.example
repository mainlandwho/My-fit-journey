# Supabase — from Project Settings > API
NEXT_PUBLIC_SUPABASE_URL=https://xgkgpjkswwrtbkstunxq.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=          # server-only, never expose to the client

# Stripe
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=              # from `stripe listen` locally, or the dashboard endpoint

# AI Coach
ANTHROPIC_API_KEY=

# Cron (renewal + abandoned-cart processors) — any long random string, must
# match what you set in Vercel's cron configuration / manual trigger header
CRON_SECRET=

# Abandoned-cart reminders (optional — reminders are logged but not sent
# until these are set)
RESEND_API_KEY=
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_FROM_NUMBER=

# App
NEXT_PUBLIC_APP_URL=http://localhost:3000
